<Window x:Class="UIPackageManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		Width="{Binding CurrentViewModel.Width}"
		Height="{Binding CurrentViewModel.Height}"
        MinWidth="{Binding CurrentViewModel.Width}"
        MinHeight="{Binding CurrentViewModel.Height}">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"></RowDefinition>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition></ColumnDefinition>
		</Grid.ColumnDefinitions>
		<ContentControl Grid.Row="0" Grid.Column="0" Content="{Binding CurrentViewModel}"/>
	</Grid>
</Window>
